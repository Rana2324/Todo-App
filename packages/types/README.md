# packages/types

Placeholder. Currently empty — domain types (`TodoType`, `Place`, …) live in `apps/web/lib/domain/` because they're only used by this one app.

Move a type here once a **second app** needs to share it (e.g. a mobile client consuming the same API shapes).
