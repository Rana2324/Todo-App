# packages/ui

Placeholder. Currently empty — `apps/web/components/ui/*` (shadcn/Radix primitives) live in the app itself because there's only one app, so nothing is actually shared yet.

Move a component here once a **second app** in this repo needs it too. At that point this becomes a real internal package apps import via a path alias.
