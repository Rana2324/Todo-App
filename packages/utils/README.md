# packages/utils

Placeholder. Currently empty — `apps/web/lib/utils.ts` (the `cn` helper) stays in the app because it's a one-line re-export with a single consumer right now.

Move a utility here once a **second app** needs the exact same helper.
